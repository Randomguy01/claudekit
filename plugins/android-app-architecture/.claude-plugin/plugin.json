{
  "name": "android-app-architecture",
  "version": "0.1.0",
  "description": "Design and structure Android apps using the official recommended app architecture — the UI, domain, and data layers, separation of concerns, single source of truth, unidirectional data flow, and dependency-injection boundaries — with task-oriented guides, plus autonomous layer-builder subagents that implement a layer end-to-end on top of the Room, Retrofit, DataStore, WorkManager, and Hilt skills."
}
